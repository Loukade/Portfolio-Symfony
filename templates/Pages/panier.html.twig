{% extends 'base.html.twig' %}
{% block body %}
    <br><br><br>
    <section>
        <h1 class="black">Votre Panier</h1><br><br>
    </section>
    {% if items | length > 0 %}
       <div class="block">
        <table class="table">
            <thead>

            <tr>
                <th>Produit</th>
                <th>Prix</th>
                <th>Quantité</th>
                <th>Total</th>
                <th></th>


            </tr>
            </thead>

            <tbody>
            {% for item in items %}
                <tr>
                    <td>{{ item.produit.nom }}</td>
                    <td>{{ item.produit.prix }}</td>
                    <td>{{ item.quantity }}</td>
                    <td>{{ item.produit.prix * item.quantity}}</td>
                    <td>
                        <a href="{{ path('panier_remove' , {'id': item.produit.id}) }}" class="btn btn-danger btn-sm">
                            <i class="fas fa fa-trash"></i>

                        </a>
                    </td>

                </tr>

            {% endfor %}
            </tbody>
<tfoot>
<tr>
    <td>Total :</td>
    <td>{{ total }}</td>
    <td></td>
</tr>
</tfoot>
        </table>
       </div>
    {% else %}
        <p>Votre panier est vide </p>
    {% endif %}

    {% include ("pages/includes/footer.html.twig") %}
{% endblock %}